import { AdminPageFrame } from "@/features/administrator/AdminPageFrame";

export default function AdministratorAuditPage() {
  return (
    <AdminPageFrame
      title="Audit log"
      description="Immutable admin activity stream with filters."
    />
  );
}
